#include <iostream>
#include <string> 
#include <deque>
#include "Candidate.h" 

/* temporary constructor for compiling purposes; values for variables will actually be based on */
Candidate::Candidate() { 
	female_votes = 61;
	male_votes = 65;
	college_votes = 22; 
	total_votes = 126;
}
