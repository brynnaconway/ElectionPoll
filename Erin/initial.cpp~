#include <iostream>
#include <fstream>
#include <vector>
#include <cstring>

using namespace std;

class Vote {
	public:
		Vote();
	private:
		vector <string> votes;
};

Vote::Vote(){
 vector <string> votes;
    list.clear();
    
    string temp;
    ifstream infile("results.dat");
    while(!(infile.eof())){
                           infile>>temp;temp+=" ";
                           list.push_back(temp);}
    
    for(int t=0;t<list.size();++t){
            cout<<list.at(t);
                 }
                 return 0;
                 }
